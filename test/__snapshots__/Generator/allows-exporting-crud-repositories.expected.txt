// AuthorBooksTable.ts :
/**
 * DO NOT EDIT:
 *
 * This file has been auto-generated from database schema using ts-sql-codegen.
 * Any changes will be overwritten.
 */
import { View } from "ts-sql-query/View";
import type { DBConnection } from "../helpers/connection-source";
import {
    Genre,
} from "../helpers/types";

export class AuthorBooksTable extends View<DBConnection, 'AuthorBooksTable'> {
    id = this.optionalColumn('id', 'uuid');
    name = this.optionalColumn('name', 'string');
    authorId = this.optionalColumn('author_id', 'int');
    releasedAt = this.optionalColumn('released_at', 'localDate');
    timeToRead = this.optionalColumn('time_to_read', 'int');
    genre = this.optionalColumn<Genre>('genre', 'enum', 'genre');
    weightGrams = this.optionalColumn('weight_grams', 'double');
    authorName = this.optionalColumn('author_name', 'string');

    constructor() {
        super('author_books');
    }
}




// AuthorsTable.ts :
/**
 * DO NOT EDIT:
 *
 * This file has been auto-generated from database schema using ts-sql-codegen.
 * Any changes will be overwritten.
 */
import { Table } from "ts-sql-query/Table";
import type { DBConnection } from "../helpers/connection-source";
import {
    extractColumnsFrom,
} from "ts-sql-query/extras/utils";
import {
    DynamicCondition,
} from "ts-sql-query/expressions/dynamicConditionUsingFilters";

export class AuthorsTable extends Table<DBConnection, 'AuthorsTable'> {
    id = this.primaryKey('id', 'int');
    name = this.optionalColumn('name', 'string');
    dob = this.optionalColumn('dob', 'localDate');
    createdAt = this.columnWithDefaultValue('created_at', 'localDateTime');
    updatedAt = this.columnWithDefaultValue('updated_at', 'localDateTime');

    constructor() {
        super('authors');
    }
}

export type AuthorsCols = {
    id: 'int'
    name: 'string'
    dob: 'localDate'
    createdAt: 'localDateTime'
    updatedAt: 'localDateTime'
}


export type AuthorsTablePk = SelectedRow<AuthorsTable>["id"];

export class AuthorsCrudRepo {
    constructor(
        public getConnection: () => DBConnection,
        public table: AuthorsTable,
    ) {}

    tableCols = extractColumnsFrom(this.table);

    select(conn = this.getConnection()) {
        return conn.selectFrom(this.table);
    }

    selectWhere(cond: DynamicCondition<AuthorsCols>, conn = this.getConnection()) {
        return this.select(conn)
            .where(conn.dynamicConditionFor(this.tableCols).withValues(cond))
            .select(this.tableCols)
            .executeSelectMany();
    }

    async findAll(conn = this.getConnection()) {
        const rows = await this.select(conn)
            .select(this.tableCols)
            .executeSelectMany();
        return rows;
    }

    async findOneById(id: AuthorsTablePk, conn = this.getConnection()) {
        const row = await this.select(conn)
            .where(this.table.id.equals(
                id
            ))
            .select(this.tableCols)
            .executeSelectNoneOrOne();
        return row
    }

    async findManyById(idList: AuthorsTablePk[], conn = this.getConnection()) {
        const rows = await this.select(conn)
            .where(this.table.id.in(
                idList
            ))
            .select(this.tableCols)
            .executeSelectMany();
        return rows;
    }

    insert(conn = this.getConnection()) {
        return conn.insertInto(this.table);
    }

    async insertOne(row: InsertableRow<AuthorsTable>, conn = this.getConnection()) {
        const result = await this.insert(conn)
            .set(row)
            .returning(this.tableCols)
            .executeInsertOne();
        return result;
    }

    async insertMany(rows: InsertableRow<AuthorsTable>[], conn = this.getConnection()) {
        const result = await this.insert(conn)
            .values(rows)
            .returning(this.tableCols)
            .executeInsertMany();
        return result;
    }

    update(conn = this.getConnection()) {
        return conn.update(this.table);
    }

    async updateOneById(id: AuthorsTablePk, update: UpdatableRow<AuthorsTable>, conn = this.getConnection()) {
        const result = await this.update(conn)
            .set(update)
            .where(this.table.id.equals(id))
            .returning(this.tableCols)
            .executeUpdateOne();
        return result;
    }

    async updateManyById(idList: AuthorsTablePk[], update: UpdatableRow<AuthorsTable>, conn = this.getConnection()) {
        const result = await this.update(conn)
            .set(update)
            .where(this.table.id.in(idList))
            .returning(this.tableCols)
            .executeUpdateMany();
        return result;
    }

    delete(conn = this.getConnection()) {
        return conn.deleteFrom(this.table);
    }

    async deleteOneById(id: AuthorsTablePk, conn = this.getConnection()) {
        const result = await this.delete(conn)
            .where(this.table.id.equals(id))
            .executeDelete();
        return result
    }

    async deleteManyById(idList: AuthorsTablePk[], conn = this.getConnection()) {
        const result = await this.delete(conn)
            .where(this.table.id.in(idList))
            .executeDelete();
        return result;
    }
}


// BooksTable.ts :
/**
 * DO NOT EDIT:
 *
 * This file has been auto-generated from database schema using ts-sql-codegen.
 * Any changes will be overwritten.
 */
import { Table } from "ts-sql-query/Table";
import type { DBConnection } from "../helpers/connection-source";
import {
    extractColumnsFrom,
} from "ts-sql-query/extras/utils";
import {
    Genre,
} from "../helpers/types";
import {
    DynamicCondition,
} from "ts-sql-query/expressions/dynamicConditionUsingFilters";

export class BooksTable extends Table<DBConnection, 'BooksTable'> {
    id = this.autogeneratedPrimaryKey('id', 'uuid');
    name = this.column('name', 'string');
    authorId = this.column('author_id', 'int');
    releasedAt = this.optionalColumn('released_at', 'localDate');
    timeToRead = this.optionalColumn('time_to_read', 'int');
    genre = this.optionalColumn<Genre>('genre', 'enum', 'genre');
    weightGrams = this.optionalColumn('weight_grams', 'double');

    constructor() {
        super('books');
    }
}

export type BooksCols = {
    id: 'uuid'
    name: 'string'
    authorId: 'int'
    releasedAt: 'localDate'
    timeToRead: 'int'
    genre: ['enum', Genre]
    weightGrams: 'double'
}


export type BooksTablePk = SelectedRow<BooksTable>["id"];

export class BooksCrudRepo {
    constructor(
        public getConnection: () => DBConnection,
        public table: BooksTable,
    ) {}

    tableCols = extractColumnsFrom(this.table);

    select(conn = this.getConnection()) {
        return conn.selectFrom(this.table);
    }

    selectWhere(cond: DynamicCondition<BooksCols>, conn = this.getConnection()) {
        return this.select(conn)
            .where(conn.dynamicConditionFor(this.tableCols).withValues(cond))
            .select(this.tableCols)
            .executeSelectMany();
    }

    async findAll(conn = this.getConnection()) {
        const rows = await this.select(conn)
            .select(this.tableCols)
            .executeSelectMany();
        return rows;
    }

    async findOneById(id: BooksTablePk, conn = this.getConnection()) {
        const row = await this.select(conn)
            .where(this.table.id.equals(
                id
            ))
            .select(this.tableCols)
            .executeSelectNoneOrOne();
        return row
    }

    async findManyById(idList: BooksTablePk[], conn = this.getConnection()) {
        const rows = await this.select(conn)
            .where(this.table.id.in(
                idList
            ))
            .select(this.tableCols)
            .executeSelectMany();
        return rows;
    }

    insert(conn = this.getConnection()) {
        return conn.insertInto(this.table);
    }

    async insertOne(row: InsertableRow<BooksTable>, conn = this.getConnection()) {
        const result = await this.insert(conn)
            .set(row)
            .returning(this.tableCols)
            .executeInsertOne();
        return result;
    }

    async insertMany(rows: InsertableRow<BooksTable>[], conn = this.getConnection()) {
        const result = await this.insert(conn)
            .values(rows)
            .returning(this.tableCols)
            .executeInsertMany();
        return result;
    }

    update(conn = this.getConnection()) {
        return conn.update(this.table);
    }

    async updateOneById(id: BooksTablePk, update: UpdatableRow<BooksTable>, conn = this.getConnection()) {
        const result = await this.update(conn)
            .set(update)
            .where(this.table.id.equals(id))
            .returning(this.tableCols)
            .executeUpdateOne();
        return result;
    }

    async updateManyById(idList: BooksTablePk[], update: UpdatableRow<BooksTable>, conn = this.getConnection()) {
        const result = await this.update(conn)
            .set(update)
            .where(this.table.id.in(idList))
            .returning(this.tableCols)
            .executeUpdateMany();
        return result;
    }

    delete(conn = this.getConnection()) {
        return conn.deleteFrom(this.table);
    }

    async deleteOneById(id: BooksTablePk, conn = this.getConnection()) {
        const result = await this.delete(conn)
            .where(this.table.id.equals(id))
            .executeDelete();
        return result
    }

    async deleteManyById(idList: BooksTablePk[], conn = this.getConnection()) {
        const result = await this.delete(conn)
            .where(this.table.id.in(idList))
            .executeDelete();
        return result;
    }
}


// ChaptersTable.ts :
/**
 * DO NOT EDIT:
 *
 * This file has been auto-generated from database schema using ts-sql-codegen.
 * Any changes will be overwritten.
 */
import { Table } from "ts-sql-query/Table";
import type { DBConnection } from "../helpers/connection-source";
import {
    extractColumnsFrom,
} from "ts-sql-query/extras/utils";
import {
    ChapterMetadataAdapter,
} from "../helpers/adapters";
import {
    ChapterMetadata,
} from "../helpers/types";
import {
    DynamicCondition,
} from "ts-sql-query/expressions/dynamicConditionUsingFilters";

/**
 * Chapters information is only available for books with parseable metadata; Consumers should not assume completeness.
*/
export class ChaptersTable extends Table<DBConnection, 'ChaptersTable'> {
    id = this.autogeneratedPrimaryKey('id', 'int');
    name = this.column('name', 'string');
    bookId = this.column('book_id', 'uuid');
    metadata = this.optionalColumn<ChapterMetadata>('metadata', 'custom', 'jsonb', ChapterMetadataAdapter);
    title = this.optionalColumn('title', 'string');
    description = this.optionalColumn('description', 'string');

    constructor() {
        super('chapters');
    }
}

export type ChaptersCols = {
    id: 'int'
    name: 'string'
    bookId: 'uuid'
    metadata: ['custom', ChapterMetadata]
    title: 'string'
    description: 'string'
}


export type ChaptersTablePk = SelectedRow<ChaptersTable>["id"];

export class ChaptersCrudRepo {
    constructor(
        public getConnection: () => DBConnection,
        public table: ChaptersTable,
    ) {}

    tableCols = extractColumnsFrom(this.table);

    select(conn = this.getConnection()) {
        return conn.selectFrom(this.table);
    }

    selectWhere(cond: DynamicCondition<ChaptersCols>, conn = this.getConnection()) {
        return this.select(conn)
            .where(conn.dynamicConditionFor(this.tableCols).withValues(cond))
            .select(this.tableCols)
            .executeSelectMany();
    }

    async findAll(conn = this.getConnection()) {
        const rows = await this.select(conn)
            .select(this.tableCols)
            .executeSelectMany();
        return rows;
    }

    async findOneById(id: ChaptersTablePk, conn = this.getConnection()) {
        const row = await this.select(conn)
            .where(this.table.id.equals(
                id
            ))
            .select(this.tableCols)
            .executeSelectNoneOrOne();
        return row
    }

    async findManyById(idList: ChaptersTablePk[], conn = this.getConnection()) {
        const rows = await this.select(conn)
            .where(this.table.id.in(
                idList
            ))
            .select(this.tableCols)
            .executeSelectMany();
        return rows;
    }

    insert(conn = this.getConnection()) {
        return conn.insertInto(this.table);
    }

    async insertOne(row: InsertableRow<ChaptersTable>, conn = this.getConnection()) {
        const result = await this.insert(conn)
            .set(row)
            .returning(this.tableCols)
            .executeInsertOne();
        return result;
    }

    async insertMany(rows: InsertableRow<ChaptersTable>[], conn = this.getConnection()) {
        const result = await this.insert(conn)
            .values(rows)
            .returning(this.tableCols)
            .executeInsertMany();
        return result;
    }

    update(conn = this.getConnection()) {
        return conn.update(this.table);
    }

    async updateOneById(id: ChaptersTablePk, update: UpdatableRow<ChaptersTable>, conn = this.getConnection()) {
        const result = await this.update(conn)
            .set(update)
            .where(this.table.id.equals(id))
            .returning(this.tableCols)
            .executeUpdateOne();
        return result;
    }

    async updateManyById(idList: ChaptersTablePk[], update: UpdatableRow<ChaptersTable>, conn = this.getConnection()) {
        const result = await this.update(conn)
            .set(update)
            .where(this.table.id.in(idList))
            .returning(this.tableCols)
            .executeUpdateMany();
        return result;
    }

    delete(conn = this.getConnection()) {
        return conn.deleteFrom(this.table);
    }

    async deleteOneById(id: ChaptersTablePk, conn = this.getConnection()) {
        const result = await this.delete(conn)
            .where(this.table.id.equals(id))
            .executeDelete();
        return result
    }

    async deleteManyById(idList: ChaptersTablePk[], conn = this.getConnection()) {
        const result = await this.delete(conn)
            .where(this.table.id.in(idList))
            .executeDelete();
        return result;
    }
}
